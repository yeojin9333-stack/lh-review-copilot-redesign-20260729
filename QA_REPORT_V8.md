# LH Review Copilot v8 QA

검증일: 2026-07-28

## 결과

- 대시보드 Viewer 높이 안정화: 1440×900과 390×844에서 기본·선택 상태를
  반복 측정했으며 Viewer, Canvas, 문서 높이 변화 0px
- 역할 선택 → LH 담당자 → 단지 BIM → B1 램프 상세 → Context → 사례 →
  법령·지침 → 대안 1·2·3 → 전문가 판단 → 설계사 답변 → LH 최종 반영 →
  타임라인 → 유사 이력의 20단계 흐름 통과
- 하단 주 버튼은 단계별로 하나만 노출되며 이전 이동 후에도 선택 대안과 펼친
  사례가 유지됨
- 대안 전환 시 이전 대안의 Ghost·색상·오버레이 잔상 없음
- 램프 상부, 곡선부, 하부, 측면에서 바닥·벽체·진행선의 단절 또는 부유 없음
- 전문가 판단, 설계사 재제출, LH 반영 상태가 동일한 업무 ID로 D1에 연결됨
- 브라우저 콘솔 오류 없음

## 반응형

- 1920×1080
- 1440×900
- 1280×720
- 390×844 모바일

네 크기에서 가로 넘침, Viewer 잘림, 사이드바 충돌, 하단 내비게이션의 콘텐츠
가림을 확인하지 못했습니다. 대표 캡처는 `qa/screenshots`에 있습니다.

## AI/RAG 상태

- 공개 기본값: `sourceMode: "rule"`인 내장 시나리오 RAG
- 외부 `REVIEW_COPILOT_API_URL` 연결 시: `sourceMode: "llm"`
- 외부 연결 실패 시: `sourceMode: "mock"`과 사용자 경고
- 비밀정보와 API 키 값은 UI와 로그에 표시하지 않음

현재 공개 기본 모드는 실제 사례·법령 데이터를 회수하지만 생성형 LLM 호출은
아닙니다. Ollama LLM+벡터 RAG는 `services/review-copilot-ai-ollama`를 별도 HTTPS
환경에 배포하고 `REVIEW_COPILOT_API_URL`을 연결해야 공개 사이트에서 사용됩니다.

## 수정한 흐름 오류

1. 같은 `/decision` 라우트 안에서 전문가 화면이 LH 화면으로 전환되지 않던
   상태 유지 문제를 명시적 화면 전환으로 수정
2. 전문가 요청과 설계사·LH 단계의 업무 ID가 달라 타임라인이 분리되던 문제를
   하나의 고정 업무 ID로 통합
3. 안내, 프로젝트 전환, 이력 가져오기, 첨부 Mock, 임시저장 버튼의 피드백과
   실제 상태 변경을 연결
