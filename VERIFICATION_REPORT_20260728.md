# BIM 중심 자동 검토 흐름 검증 보고서

기준일: 2026-07-28  
대상: `lh-review-copilot-bim-flow-v6`  
배포 상태: 공개 Sites 버전 7 재배포 및 운영 화면 확인 완료

## 구현 결과

- 첫 화면을 실시설계 상태, 대형 인터랙티브 3D BIM, 단일 판단 필요 안건으로 단순화
- 참고 이미지를 배경으로 사용하지 않고 Three.js 저복잡도 Cutaway 형상으로 구현
- 곡선형 램프, 벽체, 슬래브, 주차면, 기둥, 진행선, 트렌치, 집수정 후보,
  차단기·검지코일, 충돌방지 시설을 개별 객체로 표현
- 전체 보기, 선택 구간 보기, 현재안, 대안 1·2·3, Orbit·Pan·Zoom 지원
- AI 검토 결과를 작은 BIM과 Context·사례·법령·지침·대안 비교 탭으로 재구성
- 사례와 법령·지침을 분리하고 적용조건·원문 위치·직접 근거/AI 해석 경계를 표시
- 대안마다 BIM 형상, 동선, 시간, 비용의 정성적 영향을 비교
- 전문가 요청, 채택/보완 필요/기각, 설계사 답변, LH 최종 반영, 타임라인 유지

## LLM·RAG 연결 검증

| 항목 | 확인 결과 |
|---|---|
| 실제 Provider 호출 코드 | `OllamaProvider._request("/api/chat")`와 구조화 응답 스키마 확인 |
| 외부 연결 조건 | `REVIEW_COPILOT_API_URL` 설정 시 FastAPI/Ollama/SQLite 벡터·FTS RAG 사용 |
| 이번 운영 실행 모드 | `sourceMode: "rule"` · `scenario-rag-grounded-v1` · 문서 379건 |
| API 실행 | `/api/ai/health` GET 200, `/api/ai/review` POST 2건 모두 200 |
| Mock fallback | 이번 실행에서는 미사용. 외부 연결과 내장 API가 모두 실패할 때만 사용 |
| 프롬프트 입력 | 검토 의도, B1/R-02 대상, 시나리오 ID, 확인·미확인정보, 공간조건, 연관 객체, 대안조건 |
| 검색 결과 | VE 사례와 법령·지침 SourceReference가 검토 패키지와 화면에 반영됨 |
| 장애 표시 | `AI 연결 실패·시연 데이터 사용`과 `sourceMode: "mock"`으로 구분 |
| 비밀정보 | UI·진단 정보·로그에 API 키와 토큰 값을 포함하지 않음 |

실제 Ollama 서버가 이 작업 환경에서 실행 중이지 않았으므로 생성형 LLM 응답 자체는
호출하지 않았습니다. 대신 외부 Provider 호출 코드와 스키마, Proxy의 `sourceMode:
"llm"` 반영, 내장 RAG 실행, Mock 장애 경로를 각각 분리해 검증했습니다.

## 실행 검증

- ESLint: 통과
- vinext production build: 통과
- 렌더링·API 자동 테스트: 7/7 통과
- 공유 전 점검: 필수 파일 정상, 원본 Excel 포함, 비밀정보 의심 파일 없음
- 브라우저:
  - 대형 3D BIM WebGL canvas 정상 생성
  - 공개 사이트 메인·검토 결과 화면의 3D BIM canvas 정상 생성
  - 가로 오버플로 없음
  - Context·사례·법령·대안 탭 정상
  - 대안 1 선택 시 BIM 변경 형상 연동
  - 전문가 검토 요청 후 판단 화면 이동
  - `보완 필요` 판단 저장 성공
  - 설계사 답변, LH 최종 반영, 타임라인 화면 정상
  - 브라우저 콘솔 오류 없음

## 현재 한계

- 3D Viewer는 실제 IFC 모델 렌더러가 아니라 서비스 흐름 검증용 형상 모델입니다.
- 동선·시간·비용 영향은 수량산출·공정표·견적 결과가 아닌 정성적 검토 가설입니다.
- 실제 Ollama E2E 검증은 팀 로컬에서 Ollama와 임베딩 모델을 실행한 뒤 별도로
  확인해야 합니다.
- Three.js 포함으로 클라이언트 청크 크기 경고가 있으나 빌드는 성공하며, 객체 수와
  재질 복잡도·픽셀비를 제한했습니다. 실제 IFC를 연결할 때는 지연 로딩과 LOD가
  추가로 필요합니다.
