# LH Review Copilot v10 QA

검증일: 2026-07-29  
대상: 팀원 기대효과 기능 선별 통합본 및 기존 Sites 프로젝트 version 11  
운영 URL: https://lh-review-copilot-20260727.geonnii.chatgpt.site

## 통합 범위

- 팀원 v10에서 추가된 `기대효과 요약` 탭과 대안 선택 UI를 통합했습니다.
- 현재안·대안 1·2·3은 기존 `ReviewAlternative` 단일 데이터 객체를 공유합니다.
- 연결된 VE 사례의 `expected_effects`, 변경 객체, 연결 근거, 추가 확인사항을
  기대효과 화면에 연결했습니다.
- 근거 없이 고정된 공사비·공기·안전 점수, 위험 점수, AI 신뢰도와 권고 판정은
  통합하지 않았습니다.
- 비용·공기·성능 수치는 계산서·수량산출 근거가 있을 때만 표시한다는 경계를
  화면에 명시했습니다.

## 자동 검증

- TypeScript: 통과
- ESLint: 통과
- vinext production build: 통과
- Node 회귀 테스트: 9/9 통과
- 팀 공유 준비 점검: 통과
- 비밀정보 의심 파일: 없음

빌드 시 Three.js가 포함된 일부 클라이언트 청크의 500KB 권고 경고는 남아 있습니다.
기존 상세 BIM 구조에 따른 성능 권고이며 빌드 실패나 실행 오류는 아닙니다.

## 기대효과 회귀 확인

- `/decision` 서버 렌더링에서 `기대효과 요약`과
  `한눈에 보는 기대효과`가 표시됩니다.
- 대안 선택값 하나로 기대 변화, 변경 객체, 연결 근거, 추가 확인사항이
  함께 변경됩니다.
- 정성 영향은 긍정·조건부·중립의 색과 텍스트를 함께 사용합니다.
- `sourceMode`는 LLM·규칙·Mock 출처 상태로 구분합니다.
- 원본 데이터와 D1 스키마는 변경하지 않았습니다.

